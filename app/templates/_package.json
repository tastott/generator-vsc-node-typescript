{
	"name": "<%= projectName %>",
	  
	"version": "0.0.0",
	  
	"author": "author",

	"main": "<%= nodemain %>",

  	"scripts" : {
		"build": "tsc -p ./",
    	"test": "npm run build && node_modules\\.bin\\mocha --recursive",
		"start": "<%= nodestart %>"
	}
}